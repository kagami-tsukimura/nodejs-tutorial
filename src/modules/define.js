const ONE_SECOND = 1000;
const LOOP_COUNT = 3;
const CALL_MESSAGE = 'Hello World!';
const callLog = (message) => {
  console.log(message);
};

module.exports = { ONE_SECOND, LOOP_COUNT, CALL_MESSAGE, callLog };
